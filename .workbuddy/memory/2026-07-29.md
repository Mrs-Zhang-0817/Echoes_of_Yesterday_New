# 2026-07-29

## 签字转场优化 + 发布到原线上链接
- 优化 `sign_to_whiteboard.html` 转场：裁剪区改为紧包"走失人员签字"行（CROP ix=910,iy=772,iw=510,ih=118），缩放中心改为空白签字区中心（图片坐标 1240,806），动画 1100ms cubic-out → 1600ms ease-in-out，叠纸 60% 起渐入 + 暗角聚焦效果。
- 发布链路确认：线上应用为妙搭 html 应用 `app_17b0s9c0h90`（名"昨日重现-Ch2-拼图关卡"，无 .spark/git，走旧版 `lark-cli apps +html-publish` 直传链路）。
- 发布方法：组 `.publish/` 目录（dist/index.html 拼图主页自包含 + sign_to_whiteboard.html + assets/images 含 sign_scene.png），`+html-publish --app-id app_17b0s9c0h90 --path ./.publish --as user`。
- 线上验证 200：主页 `/app/app_17b0s9c0h90`，签字页 `/app/app_17b0s9c0h90/sign_to_whiteboard`（平台会去 .html 后缀重定向），资源图正常。
- 注意：CloudStudio 部署的临时链接（6cefc7c0…agentos-app.net）用户不要，以妙搭原链接为准。

## 审查 GitHub 仓库 Echoes_of_Yesterday（只读，未改代码）
- 审查 `Mrs-Zhang-0817/Echoes_of_Yesterday` main 分支，结论存 `/tmp/echo_review/CODE_REVIEW.md`。
- 🔴 严重：仓库源码 `src/main.js` 加载 `room_bg.png`/`desk_bg.png`，但实际资产是 `scene_room.jpg`/`scene_desk.jpg` → 任一 404 即 `Loader.loadImages` reject → Ch2 拼图黑屏进不去。
- 🔴 卫生：`.pnpm-store/`(pnpm 缓存整包) + `showcase-demo/`(几乎相同副本) 被误提交，`.gitignore` 未覆盖；`dev-*.log` 泄露开发者本地路径/姓名。
- 🟡 孤儿：`src/phase4.js`/`src/phase5.js` 无入口引用；三套平行实现(Canvas游戏/DOM记忆报告/Next.js React版)架构混乱；根 README 严重失实、SKILL 吸附半径(36)与代码(50)漂移。
- ✅ 好：无硬编码密钥(全仓扫描零命中)、拼图核心算法扎实、有 tests/。
- 注意：本仓库与用户工作区"抖音大区赛"是同一项目不同 fork 态，审查以 GitHub 提交为准。

## 清洗 Echoes_of_Yesterday 仓库（按审查结论，直接在主分支操作）
- 回滚点：打 tag `rollback-before-cleanup-20260729` 指向清理前 HEAD `29b422f3`（仍可 `git reset --hard` 回滚）。
- 做法：用 GitHub Git Data API 重建 tree（仅 14 个游戏文件）→ 单次 commit `502e2dc6` 更新 main。未改历史结构，仅替换工作树内容。
- 保留（游戏本体）：`index.html` + `src/main.js`(已修路径) + `src/core/*`(4) + `src/scenes/*`(5) + `assets/images/scene_{room,desk,puzzle}.jpg`(3)。
- 删除（约 274 文件）：`.pnpm-store`、`showcase-demo`、`MemoryReport`、`Design_Document`、`美术素材`、`记忆恢复报告新底图`、`pictures`、`docs`、`tests`、`.claude`、根目录 md/docx/html、`package.json`、`src/memory-report*`、`src/phase4.js`、`src/phase5.js`、`src/ui/*`、`src/components/*`、`src/animations/*`、`src/data/*`、孤儿 `src/styles/*`、`assets/memory_items`、`assets/ui`、`assets/fonts`。
- 修复：main.js manifest `room_bg.png/desk_bg.png` → `scene_room.jpg/scene_desk.jpg`（黑屏根因）。
- 体积：~102.9MB → ~5.7MB（3 张背景图占主体，未重压缩以保画质）。
- 校验：递归 tree 19 项(5 目录+14 blob)、提交 main.js blob 解码确认路径修复、tag 指向旧 HEAD，均通过。
- 备注：用户指令"只留游戏本体"故未加 .gitignore/README；若想防止 .pnpm-store 复发可后续补 .gitignore。

## 本地克隆 + 回滚到原始版本
- 克隆到工作区 `当前项目/抖音大区赛/Echoes_of_Yesterday`（用 `gh repo clone` 成功；git 原生 clone 走沙箱代理 502 失败，gh 自己的 HTTP 通道可用——复用此方式）。
- 已 `git checkout rollback-before-cleanup-20260729` → detached HEAD `29b422f`（清理前原始完整版，204 文件，含 .pnpm-store 等）。远程 main 仍是清洗版 `502e2dc` 未动。
- 回到清洗版：`git -C Echoes_of_Yesterday checkout main`。

## 远程 main 已回退到原始版本（用户确认）
- 用户澄清"退回到原来的版本"指 GitHub 远程仓库（非仅本地）。确认后执行：
- 保护 tag `cleaned-20260729` → `502e2dc6`（清洗版，防回退后丢失）。
- 远程 `main` 强制回退（force PATCH git/refs/heads/main）→ `29b422f3`（原始完整版）。
- 现两个 tag 均在：`rollback-before-cleanup-20260729`(原始) 与 `cleaned-20260729`(清洗版)，随时可切换。
- 本地克隆已 `git fetch`：origin/main=29b422f3；本地 main 仍=502e2dc6(清洗版)；HEAD detached @ 29b422f3(原始)。

## 10-chapter-plan 交叉审核 + v2 重写（15:30）
- 4 路审核（合规/技术/事实/叙事PM）+ 本地实证，产出 `docs/plan-review-2026-07-29.md`。9 项🔴：产物结构自相矛盾（"单文件全内联" vs 图片相对路径；平台规则其实允许多文件 zip，禁 ES Modules 的是妙搭不是抖音）、Ch5 门牌 503 零线索、时间赤字零缓冲、Phase2.4 验收逻辑不可执行、单文件+4人并行+根目录无 git、npm test 假验收（只覆盖 puzzleLayout 纯函数）、上传链路零演练、资产盘点失实（memory-report 在 Echoes_of_Yesterday/ 且是 DOM+CSS 方案；新版底图仅 3 张；scene_desk.jpg≠desk_bg.png）、puzzle_img.png 缺 IEND 已损坏（正品 scene_puzzle.jpg 420K，千万别留错）。
- 重写 `docs/10-chapter-plan.md` v2：Phase 0 上传演练前置（D 不再空转）；骨架+占位章节 17:00 即完整可交付；模块化 chapters/chNN.js + build.js 拼接防并行冲突；记忆数值表 5→15→22→30→40→52→60→72→85→100；Ch4 登记单埋 503 线索；所有关卡 3 次失败自动辅助；签字 PASS_SCORE 降 60；ctx.filter 改离屏预生成兜底；文案禁"游戏"字样。
- 用户确认两点：① 敏捷开发——**现在就串联骨架（walking skeleton），不等关卡独立开发完**；② **包体大小团队自理**，已从计划关键路径移除（0.5 压缩、4.1 包体核对、第六节预算表全部降级，开发直接用原图；仅保留"拼图必须用 scene_puzzle.jpg 正品"一条硬约束）。
- 下一步：按 v2 实施，Phase 0（上传演练+资产清理）→ Phase 1 骨架（ChapterManager+10 占位章节+build.js）。

## 1~10 关实测找 bug（17:10-17:30）
- 方法：3 路智能体并行代码审查（core+ch01-03 / ch04-06 / ch07-10+build）+ Playwright 无头实机试玩（冷启动/逐章fuzz/强制完成链/针对复现）。测试对象 build_out/index.html。脚本留存 /tmp/echo_playtest/playtest{,2}.mjs（内置静态服务器版——沙箱后台 python http.server 会被回收，服务器必须与测试同进程）。
- 报告：`docs/playtest-bug-report-2026-07-29.md`。
- 实锤 P0：① ch08 handleDown 在 writing 阶段短路 checkButtonClick → 提交按钮死、超时弹层无法消除，第8关无法通关；② ch05/06/07 调 `progress.save('chXX_complete',true)` 把整份存档覆盖成字符串（save 是全量接口）→ 通关即毁档回 ch01；③ 三章完成 overlay 每帧重复 show + Overlay.hide() 回调 onEnter → 弹层跨章残留劫持输入；④ ch04-07 完成未 markChapterComplete → ch10 报告显示未完成。
- 实锤 P1：刷新重玩已通关章（chapter 存本章非下一章）；ch10 重开跳到 ch02 漏 ch01；记忆数值三套不一致；Loader 1×1 占位图会让 ch02 getImageData 崩；roundRect 无 polyfill。
- 好消息：fuzz 全程 0 控制台报错，ch01→04、08→10 完成链正常。
- 注意：window.game 已暴露，可控制台跳关（chapterManager.activate('chXX')），测试利器。

## 美术素材盘点与安装（17:48-17:56）
- 交叉比对本地 asset-inventory.md 与飞书《比赛备忘录·美术资源总览36张》/《进展报告0729》，口径一致
- 从 Echoes_of_Yesterday 旧仓库安装 28 个素材到 assets/images/（中文名统一转 ASCII：report_photo_chN / report_bg_chN / sign_panel_vN / main_menu_bg）
- 修复 Ch3 迷宫图缺失 bug（scene_maze_map.png 补回主 assets）
- 代码接入：main_new.js manifest 新增 mainMenuBg/reportBase/paperBase；Ch1 背景用主界面底图+压暗遮罩；Ch10 报告底板用 report_base.png；均带 _placeholder 程序化兜底
- 重要发现：旧仓库拼图.png 原件完好（本地损坏是复制事故），存为 puzzle_img_fixed.png；旧仓库客厅场景底图/桌面与本地同名文件 MD5 不同，未覆盖
- npm test 3 个失败为存量问题（stash 验证），与本次无关
- 产出：docs/missing-assets.md（空缺素材完整清单）；assets/images 现 78MB，超 8MB zip 限制，上传前需按需打包
