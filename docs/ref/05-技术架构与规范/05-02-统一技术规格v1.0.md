---
source: docs/unified-tech-spec-v1.0.md
date: 2026-07-29
status: 当前有效 — 已反映在现有骨架代码中
related: 05-01-开发规范v1.0.md, 05-05-章节数据配置.md
---

# 05-02 统一技术规格 v1.0

## 概述

本文件记录《昨日重现》项目从零散模块到统一架构的规划。覆盖现状盘点、核心技术选型、抖音规则映射、三个已有关卡的重写/移植方案、命名约定、坐标单位以及打包规则。

---

## 一、现状盘点

### 1.1 已有模块（3/10 关卡有互动代码）

| 模块 | 来源 | 代码行数 | 架构 |
|------|------|----------|------|
| Ch2 拼图 | `src/scenes/Scene_Puzzle.js` + `puzzleLayout.js` + `src/core/*` | ~900 行 | Canvas 引擎，模块化 |
| Ch3 迷宫连线 | `Scene_Maze.js` + `mazeLayout.js` | ~460 行 | 同引擎接口（onEnter/update/render） |
| Ch8 签字 | `sign_to_whiteboard.html` / `whiteboard.html` | 287/452 行 | **独立架构**，自带 rAF + setInterval + DPR |

### 1.2 关键问题

| 问题 | 影响 |
|------|------|
| Ch8 签字是独立 app（自带 rAF/DPR/事件系统） | 无法直接接入 ChapterManager 骨架 |
| Ch2/Ch3 引擎一致但缺少 ChapterManager | 无法串联为完整流程 |
| 三个模块各自写了事件绑定和 Canvas 尺寸管理 | 代码重复，维护负担 |
| 产物形态不一致（Ch2 是单文件合并，开发环境是 ES Modules） | 打包策略需统一 |

---

## 二、目标产物形态

```text
build_out/
├── index.html           # 唯一入口，所有 JS/CSS 全部内联进一个 <script>
└── assets/images/       # 相对路径引用的压缩图片
    ├── scene_puzzle.jpg
    ├── scene_maze_map.png
    ├── sign_scene.png
    ├── scene_room.jpg / scene_desk.jpg
    └── chXX_bg.jpg ...
```

- zip 根目录 = `index.html` 直接可见
- 无 `__MACOSX`、`.DS_Store`
- zip 总体积 < 8MB

---

## 三、核心技术选型

| 决策 | 选择 | 理由 |
|------|------|------|
| 渲染方案 | **HTML5 Canvas 2D**（整个作品一个 canvas） | 拼图和迷宫已验证，跨平台一致，无 CSS 兼容问题 |
| 分辨率 | **1280x720 逻辑分辨率**，等比缩放 | 已用全项目 |
| DPR | canvas backing store 按 `width*dpr` 设，CSS 等比缩放 | fix-log 已验证方案 |
| 输入 | **Pointer Events**（pointerdown/move/up/cancel），统一 touch + mouse | InputManager 已验证 |
| 架构 | **ChapterManager** 驱动，每章 = 一个 Scene 类 | 与现有 SceneManager 模式兼容 |
| 打包 | JS/CSS 全内联单 `<script>`，用 `build.js`（Node，无依赖）拼接 | 妙搭不认 ES Modules |

### 抖音规则映射 — 技术禁止清单

- **禁止** `alert()` / `confirm()` / `prompt()` → 替代方案：Canvas 自绘弹层
- **禁止** `eval()` / `new Function()` → 不存在使用场景
- **禁止** 外部网络请求 → 全部使用本地资源
- **禁止** 外部跳转 / `<iframe>` → 不存在使用场景
- **禁止** 面向用户文案出现"游戏"字样 → 用"章节""体验""关卡"

### 兜底规则

- `ctx.filter` 不可靠（iOS Safari < 18）→ 褪色/灰度等效果用离屏 Canvas 预生成
- 图片加载失败 → 内置重试 1 次 → 失败显示自绘占位
- 所有章节统一 `try/catch` 包裹 → 自绘错误弹层
- `navigator.vibrate` 用 `try/catch` 包裹（iOS 不支持时静默跳过）

---

## 四、三个已有关卡的重写/移植方案

### 4.1 Ch2 拼图 — 移植

已按 Chapter 接口实现。需改动：

- 内部 `game.sceneManager.switchTo()` → 改为 `game.chapterManager.next()`
- 褪色效果使用离屏 Canvas 预生成灰度版（替代 `ctx.filter='saturate(...)'`）
- 完成回调从 `game.onPuzzleComplete` → 触发 `game.overlay.show()` 结算

### 4.2 Ch3 迷宫 — 移植

已按统一接口实现（onEnter/update/render 全有）。需改动：

- 节点坐标基于地图图片微调
- 完成回调接入 ChapterManager + Overlay
- 提示文案审核（无"游戏"字样）

### 4.3 Ch8 签字 — 重写

独立 app 需重写为 Chapter 接口。保留/重写清单：

**保留：**
- 笔画分类算法（classifyStroke — 横/竖/撇/捺/点/折）
- 畸变规则（横↔竖、撇↔捺）
- 匹配评分逻辑（matchStr）
- 纸纹渲染

**重写：**
- 独立 rAF → 接入 Game.gameLoop（update 回调）
- 独立 setInterval 计时 → 用 dt 累加实现
- 独立 DPR 管理 → 用全局 resizeCanvas
- DOM overlay → Canvas Overlay
- addEventListener → InputManager.setHandlers

---

## 五、进度优先级

| 优先级 | 任务 | 验收标准 |
|--------|------|----------|
| P0 | ChapterManager + ProgressStore + Overlay 核心 | 切换 2 章不报错，进度可存可取 |
| P0 | Ch2 拼图移植到 Chapter 接口 | 拖拽+吸附+褪色，npm test 10/10 |
| P0 | 占位章节（Ch1/4/5/6/7/9/10）注册，长按 1s 切章 | 全线 1→10 可导航 |
| P0 | build.js 拼接脚本 | 输出单文件 index.html |
| P1 | Ch3 迷宫移植 | 连线正确，死胡同检测 |
| P1 | Ch8 签字重写 | 写"向阳"匹配通过 |
| P1 | Ch1 镜前特效 | 点击碎裂粒子 |
| P1 | Ch4 警局 + Ch5 归家 | 双热区+数字键盘 |
| P2 | Ch6/7/9 关卡 | 各关可过 |
| P2 | Ch10 记忆报告 | 10 章进度展示+淡出 |

---

## 六、命名约定

- 文件名小写下划线：`ch02_puzzle.js`、`chapter_manager.js`
- 类名 PascalCase：`Chapter02`、`ChapterManager`
- 常量 UPPER_SNAKE：`DESIGN_W = 1280`、`DESIGN_H = 720`
- localStorage key 前缀：`ye_v1`
- 面向用户文案：`"继续下一章节"` 而非 `"下一关"`

## 七、坐标与单位

- 所有开发坐标系 = **1280x720 逻辑像素**
- 图片原图尺寸各不同，通过缩放适配
- DPR 只在 resizeCanvas 中处理，各章节代码不碰 DPR
- 输入坐标已由 InputManager 转换为逻辑坐标

## 八、build.js 拼接规则

```javascript
// 拼接顺序：
// 1. index.html 的 <!DOCTYPE>...<canvas>...<script>
// 2. core/*.js（Game, InputManager, Loader, ChapterManager, ProgressStore, Overlay）
// 3. utils/*.js（sceneUtils, puzzleLayout）
// 4. chapters/*.js（全部章节按编号顺序）
// 5. main.js（入口 + 初始化逻辑）
// 6. </script></body></html>

// 把 import/export 语句替换为直接引用
// （简单方案：concat 所有文件，去掉 import/export 行，
//   用全局 game 对象传递依赖）
```
