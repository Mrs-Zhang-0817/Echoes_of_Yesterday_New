---
source: assets/sfx/音效清单.md, assets/sfx/manifest.json
date: 2026-07-30
status: generated (未挂接)
related: assets/sfx/manifest.json
---

# 音效清单与状态

## 音效清单总览

13 种音效，均为 numpy 程序化合成 → ffmpeg 编码，提供 Opus + MP3 双格式。
整包（13 音效 × 2 格式 + manifest.json + 音效清单.md）合计约 **128 KB**。

| 音效名 | 使用场景 | 触发时机 | 时长 | Opus 大小 | MP3 大小 |
|---------|---------|---------|------|----------|---------|
| sfx_mirror_crack | Ch1 镜面裂纹显现 | `startShattering()` 后 0.3s | 0.30s | 0.68 KB | 1.62 KB |
| sfx_mirror_shatter | Ch1 镜子碎裂（高潮） | 点击镜面 / 累计 3 次点击 | 1.35s | 2.77 KB | 5.70 KB |
| sfx_ch1_complete | Ch1 完成轻铃 | `update()` 2s 后进入 complete | 1.15s | 2.73 KB | 4.86 KB |
| sfx_pickup | Ch2 碎片拾起 | `handleDown()` 拿起碎片 | 0.13s | 0.38 KB | 0.92 KB |
| sfx_snap | Ch2 碎片吸附到位 | `finishPlacement()` 吸附成功 | 0.20s | 0.60 KB | 1.20 KB |
| sfx_puzzle_fade | Ch2 色彩褪去 | `puzzleFadeOut` 阶段 | 0.85s | 2.28 KB | 3.73 KB |
| sfx_flashback | Ch2/Ch3 记忆闪回 | 帧切换 / 闪回阶段 | 0.95s | 1.93 KB | 4.16 KB |
| sfx_node_select | Ch3 节点选中 | `handleDown()` 触碰到起点 | 0.10s | 0.37 KB | 0.78 KB |
| sfx_route_wrong | Ch3 路线错误 | wrong 阶段（撞死胡同红闪） | 0.42s | 0.83 KB | 2.05 KB |
| sfx_route_success | Ch3 找到成功 | `successHold`（"找到了！"） | 0.62s | 1.61 KB | 2.89 KB |
| sfx_route_fade | Ch3 路线淡出 | `routeFadeOut` 阶段 | 0.70s | 1.60 KB | 3.17 KB |
| sfx_paper_press | UI 纸张按压（按钮） | `Button.js` 派发 `paper-sound-request` 事件 | 0.16s | 0.47 KB | 1.06 KB |
| sfx_page_turn | 漫画/闪回翻页 | 漫画帧、闪回帧切换过渡 | 0.50s | 0.92 KB | 2.33 KB |

## 目录文件结构

```
assets/sfx/
├── manifest.json                  # 机器可读映射（名称→标签/时长）
├── 音效清单.md                    # 本文档
├── sfx_mirror_crack.opus / .mp3
├── sfx_mirror_shatter.opus / .mp3
├── sfx_ch1_complete.opus / .mp3
├── sfx_pickup.opus / .mp3
├── sfx_snap.opus / .mp3
├── sfx_puzzle_fade.opus / .mp3
├── sfx_flashback.opus / .mp3
├── sfx_node_select.opus / .mp3
├── sfx_route_wrong.opus / .mp3
├── sfx_route_success.opus / .mp3
├── sfx_route_fade.opus / .mp3
├── sfx_paper_press.opus / .mp3
└── sfx_page_turn.opus / .mp3
```

合计 26 个音频文件（13 音效 × 2 格式）+ 2 个元数据文件。

## 状态说明

### 已生成 + 已就位 ✅

- 全部 13 种音效的 MP3 和 Opus 版本均已生成，存储在 `assets/sfx/`
- manifest.json 文件已包含所有音效的标签和时长映射
- 播放建议：浏览器/抖音 webview 优先用 MP3 版（兼容性最稳）；Opus 版 iOS 15 以下无法解码

### 未挂接到游戏事件 ⚠️

- 当前所有音效文件已生成，但**尚未接入游戏运行时**
- 需要添加轻量 `AudioManager`，按 `manifest.json` 将音效绑定到代码触发点
- 接入后需要验证延迟同步（特别是 Ch1 镜面裂纹 0.3s 时序音效）

### 待办事项

1. 创建 `src/core/AudioManager.js`（或类似模块）
2. 在 `Loader.js` 初始化阶段加载 `manifest.json`，建立音效映射
3. 在各章节对应触发点调用 `AudioManager.play('sfx_name')`
4. 优先接入 Ch1~Ch3 的音效（已覆盖最密集的互动章节）
5. 《让我们荡起双桨》片尾曲待接入（当前无此音频文件，需另行处理）

### 使用建议

- **游戏接入**：优先用 MP3 版（兼容性最稳），Opus 适合 iOS 15+/现代浏览器
- **剪辑用途**：进剪映 / DaVinci 等直接使用 MP3 版
- **调音**：如某个音效偏尖/偏闷或长短不合适，可单独重生成，不影响其他文件
